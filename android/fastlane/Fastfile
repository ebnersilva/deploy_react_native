default_platform(:android)

platform :android do 
  lane :beta do
    

    gradle(
      task: 'assemble',
      build_type: 'Release',
      versionCode: 14
    )


    upload_to_play_store(track: 'beta')
  end
end


